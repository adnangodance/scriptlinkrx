
  # Build responsive product UI

  This is a code bundle for Build responsive product UI. The original project is available at https://www.figma.com/design/cdMzpqqdjEY74Pl9h0hbgK/Build-responsive-product-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  